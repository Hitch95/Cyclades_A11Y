import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#FAF7F4',
      navbar: '#9C2D41',
      footer: '#9C2D41',
    },
  },
  components: {},
});

export default theme;
